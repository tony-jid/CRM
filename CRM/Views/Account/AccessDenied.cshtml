@{
    ViewData[nameof(EnumViewData.Title)] = "Access denied";
    Layout = "~/Views/Shared/_Layout_Auth.cshtml";
}

<header>
    <h2 class="text-danger">@ViewData[nameof(EnumViewData.Title)]</h2>
    <p class="text-danger">You do not have access to this resource.</p>
</header>
